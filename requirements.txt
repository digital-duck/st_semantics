streamlit
numpy>=1.21.0,<2.0.0  # Keep compatible with older packages if needed
pandas>=2.1.4,<3.0.0  # Updated for docling compatibility
plotly>=6.1.1  # 5.13.0
scikit-learn>=1.0.0
torch>=2.6.0  # Security upgrade for CVE-2025-32434
torchvision>=0.19.0
transformers>=4.30.0
# laser-encoders  # >=0.0.4
# laserembeddings[zh]>=1.1.1  # Disabled due to torch compatibility issues
umap-learn>=0.5.3
phate>=1.0.7
python-dotenv>=0.19.0
requests>=2.31.0
sentencepiece>=0.1.99
kaleido>=0.2.1
deepl>=1.15.0
pillow>=7.1.0  # Streamlit compatibility
altair>=4.0.0  # streamlit-aggrid compatibility
streamlit-echarts>=0.4.0  # Apache ECharts integration for interactive visualizations
selenium>=4.15.0  # Browser automation for automatic PNG export (optional)
webdriver-manager>=4.0.0  # Automatic webdriver management (optional)

# Security upgrade notes:
# - torch>=2.6.0 required for CVE-2025-32434 fix
# - laserembeddings removed due to torch<2.0.0 requirement conflict
# - pandas upgraded for docling/docling-core compatibility
# - streamlit upgraded to 1.50.0 for better stability